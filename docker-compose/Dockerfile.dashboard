# Dashboard Dockerfile
FROM node:20-alpine

WORKDIR /app

# Install dependencies
COPY package*.json ./
RUN npm ci

# Copy source
COPY . .

# Expose Vite dev server port
EXPOSE 5173

# Run in development mode with host binding for Docker
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
